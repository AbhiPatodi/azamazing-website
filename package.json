{
  "name": "azamazing-contact-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for contact form submissions",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "wrangler": "^3.45.0"
  },
  "dependencies": {
    "itty-router": "^4.0.0",
    "nodemailer": "^6.9.7"
  }
}
